multiple ins/outs/merge tests

hot swap nodes

export topo

methods to create topo, change connections

stats method (per node, per topo)

stop/pause/restart nodes

remote hosts

variable arg counts to pass in opts to runner and topology. 
- debug to console or file
- watch node files for reload on change

pass commands to daemonized topo
